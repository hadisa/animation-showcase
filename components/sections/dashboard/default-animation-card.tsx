import { BaseAnimationCard } from "./base-animation-card"

export const DefaultAnimationCard = (props: any) => {
  // Use the base card without any customizations
  return <BaseAnimationCard {...props} />
}